Sources for the plugin `CoronaProvider.ads.vungle`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "CoronaProvider.ads.vungle" = {
            publisherId = "com.vungle",
        },
    },
}
```
